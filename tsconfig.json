{
    // https://www.typescriptlang.org/tsconfig/#extends
    // https://nuxt.com/docs/guide/concepts/typescript
    "extends": "./.nuxt/tsconfig.json",
    
    "compilerOptions": {
        // types フォルダーに型定義を入れるため：
        "types": ["@types/node", "@nuxt/types"],
        "typeRoots": ["./types"]
    }
}
